
version: 2

models:
    - name: dim_hosts
      description: "Curated Layer for Airbnb Host Dimension"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
